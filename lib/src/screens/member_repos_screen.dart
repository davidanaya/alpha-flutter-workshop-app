import 'package:flutter/material.dart';

class MemberReposScreen extends StatelessWidget {
  // TODO inject the provider and the member name

  @override
  Widget build(BuildContext context) {
    // TODO retrieve the list of repos and display them using FutureBuilder
    return Scaffold(appBar: AppBar(), body: Text('MemberReposScreen'));
  }
}
